# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /all/stats                           org.enfor.controllers.ReviewStatisticController.getStatisticsSummary
GET     /:airport/stats                           org.enfor.controllers.ReviewStatisticController.getStatistic(airport)
GET     /:airport/reviews                           org.enfor.controllers.ReviewController.getOrderedReviews(airport)

